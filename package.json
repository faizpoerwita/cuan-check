{
  "name": "cuan-check",
  "version": "1.0.0",
  "description": "Financial insights app",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.4.1"
  }
}
